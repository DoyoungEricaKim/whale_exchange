const cheerio = require('cheerio');
const request = require('request');
const promise = require('promise');

var resultModule = require("exRate.js");
console.log(resultModule);
resultModule.find().then(function(result));
console.log(resultModule);
